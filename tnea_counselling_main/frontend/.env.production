# Production environment variables
# In production, the built frontend is served directly by the FastAPI backend.
# Leaving VITE_API_URL empty ensures that all fetch calls use the same origin 
# (e.g., /recommend instead of http://localhost:8000/recommend), avoiding CORS issues.
VITE_API_URL=
